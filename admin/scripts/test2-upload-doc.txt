#!/bin/bash

# run as ./admin/scripts/update-web.sh os_directory

. ./admin/scripts/compile-config/common.sh
. ./admin/scripts/compile-config/doxygen/config.sh

rm -rf ./doc/$WEB_DOC_SUFFIX
mv ./doc/report ./doc/$WEB_DOC_SUFFIX

std_timeout 600 ./admin/scripts/update-web.sh

cd doc && rsync --rsh="ssh -x -l $WEB_USER" --modify-window=2 -lavzP . $WEB_SERVER:$WEB_DOC_DIR

