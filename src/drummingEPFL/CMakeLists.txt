PROJECT(Crawling)

FIND_PACKAGE(YARP)
FIND_PACKAGE(ICUB)

FIND_PACKAGE(CTRLLIB REQUIRED)
INCLUDE_DIRECTORIES($(CTRLLIB_INCLUDE_DIRS))

FIND_PACKAGE(IKIN REQUIRED)
INCLUDE_DIRECTORIES(${IKIN_INCLUDE_DIRS})

IF(NOT IPOPT_FOUND)
   MESSAGE(FATAL_ERROR "IPOPT is strictly required!")
ENDIF(NOT IPOPT_FOUND)


ADD_SUBDIRECTORY(DrumManager)
ADD_SUBDIRECTORY(DrumGenerator)
ADD_SUBDIRECTORY(DrumIKin)
ADD_SUBDIRECTORY(DrumIKinCartesianSolver)
ADD_SUBDIRECTORY(DrumHeadControl)
