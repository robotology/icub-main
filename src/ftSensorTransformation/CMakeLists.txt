SET(PROJECTNAME iFC)

PROJECT(${PROJECTNAME})

FIND_PACKAGE(YARP)
FIND_PACKAGE(ICUB)
FIND_PACKAGE(IKIN REQUIRED)
INCLUDE_DIRECTORIES(${IKIN_DIR}/include/iCub ${PROJECT_SOURCE_DIR} ${IKIN_INCLUDE_DIRS})
#INCLUDE_DIRECTORIES(${IKIN_INCLUDE_DIRS}, ${PROJECT_SOURCE_DIR}, ${IKIN_INCLUDE_DIRS}\iCub)

FIND_PACKAGE(CTRLLIB REQUIRED)
INCLUDE_DIRECTORIES(${CTRLLIB_INCLUDE_DIRS})

SET(folder_source src/iFC.cpp)
SET(folder_header include/iFC.h)

INCLUDE_DIRECTORIES(${FOLDER_HEADER})
SOURCE_GROUP("Source Files" FILES ${folder_source})
SOURCE_GROUP("Header Files" FILES ${folder_header})
SET(PROJECT_BINARY_DIR /bin)

ADD_LIBRARY(${PROJECTNAME} ${folder_source} ${folder_header})
INSTALL_TARGETS(/lib iFC)
#DESTINATION_FOLDER(${PROJECTNAME} bin)
#ADD_EXECUTABLE(${PROJECTNAME} ${folder_source} ${folder_header})



