SET(PROJECTNAME frameGrabberGui2)
PROJECT(${PROJECTNAME})

FILE(GLOB_RECURSE folder_header include/*.h)
FILE(GLOB_RECURSE folder_source src/*.cpp)
SOURCE_GROUP("Header Files" FILES ${folder_header})
SOURCE_GROUP("Source Files" FILES ${folder_source})

ADD_DEFINITIONS(${GTKMM_C_FLAGS})
INCLUDE_DIRECTORIES(${GTKMM_INCLUDE_DIR} ${YARP_INCLUDE_DIRS} ${PROJECT_SOURCE_DIR}/include)

ADD_EXECUTABLE(${PROJECTNAME} ${folder_header} ${folder_source})

TARGET_LINK_LIBRARIES(${PROJECTNAME} ${GTKMM_LINK_FLAGS} ${YARP_LIBRARIES})

IF (WIN32 AND NOT CYGWIN)
	SET_TARGET_PROPERTIES(${PROJECTNAME} PROPERTIES COMPILE_FLAGS /wd4250)
ENDIF (WIN32 AND NOT CYGWIN)

INSTALL(TARGETS ${PROJECTNAME} DESTINATION bin)