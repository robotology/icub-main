project(iCubGui)

if(ICUB_USE_Qt3)
	if(ICUB_USE_OpenGL)
		if(ICUB_USE_Glut)
			subdirs(src)
		else(ICUB_USE_Glut)
			message("Glut not in use, skipping iCubGui")
		endif(ICUB_USE_Glut)
	else(ICUB_USE_OpenGL)
		message("OpenGL not in use, skipping iCubGui")
	endif(ICUB_USE_OpenGL)
else(ICUB_USE_Qt3)
	message("Qt3 not in use, skipping iCubGui")
endif(ICUB_USE_Qt3)