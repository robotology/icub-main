# GUI's Gtk
IF (CREATE_GUIS_GTK)
    ADD_SUBDIRECTORY(robotMotorGui)
    IF (OPENCV_FOUND)
        ADD_SUBDIRECTORY(fingertipGui)
    ENDIF (OPENCV_FOUND)
    #ADD_SUBDIRECTORY(iCubSkinGui)
ENDIF (CREATE_GUIS_GTK)

IF (CREATE_GUIS_GTKMM)
  IF (COMPILE_NEW_FRAMEGRABBERGUI)
    ADD_SUBDIRECTORY(frameGrabberGui2)
  ELSE (COMPILE_NEW_FRAMEGRABBERGUI)
    ADD_SUBDIRECTORY(frameGrabberGui)
  ENDIF (COMPILE_NEW_FRAMEGRABBERGUI)
ENDIF (CREATE_GUIS_GTKMM)

# GUI's Qt
IF (CREATE_GUIS_QT)
    ADD_SUBDIRECTORY(qControlBoardGui)
    #ADD_SUBDIRECTORY(qControlBoardAnalysisGui)
    #ADD_SUBDIRECTORY(qSalienceGui)

    ADD_SUBDIRECTORY(libYARP_QWidgets)
    ADD_SUBDIRECTORY(libICUB_QWidgets)
    ADD_SUBDIRECTORY(qViewerGui)
    ADD_SUBDIRECTORY(applicationGui)

    ADD_SUBDIRECTORY(iCubGui)

ENDIF (CREATE_GUIS_QT)
