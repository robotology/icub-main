PROJECT("filtexamples")

SET(YARP_DIR "$ENV{YARP_ROOT}" CACHE LOCATION "where is yarp?")
SET(ICUB_DIR "$ENV{ICUB_DIR}" CACHE LOCATION "where is iCub?")

FIND_PACKAGE(ICUB REQUIRED)
FIND_PACKAGE(YARP REQUIRED)
FIND_PACKAGE(OpenCV REQUIRED)
INCLUDE_DIRECTORIES(${OPENCV_INCLUDE_DIR})
LINK_LIBRARIES(${OPENCV_LIBRARIES})
FIND_PACKAGE(FASTFILT REQUIRED)
INCLUDE_DIRECTORIES(${FASTFILT_INCLUDE_DIRS})
LINK_LIBRARIES(${FASTFILT_LIBRARIES})
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR})


ADD_EXECUTABLE(gaussfilt gaussfilt.cpp)
ADD_EXECUTABLE(gaborfilt gaborfilt.cpp)
ADD_EXECUTABLE(scalespace scalespace.cpp)
