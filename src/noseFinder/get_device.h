
#include <yarp/dev/PolyDriver.h>

// set device dd up according to arguments
// returns 0 on success, error code on failure
extern int get_device(yarp::dev::PolyDriver& dd, int argc, char *argv[]);

