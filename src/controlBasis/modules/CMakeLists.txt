#/*
# * Copyright (C) 2010 Stephen Hart. All rights reserved.
# * 
# */
cmake_minimum_required(VERSION 2.6)

SET(PROJECTNAME controlBasisModules)
PROJECT(${PROJECTNAME})

SET(CB_RESOURCE_MODULE controlBasisResource)
SET(CB_ICUB_RESOURCE_MODULE iCubControlBasisResources)
SET(CB_CONTROL_MODULE controlBasisControlLaw)
SET(CB_SCHEMA_MODULE controlBasisSchema)

#FIND_PACKAGE(ICUB QUIET)
#IF (NOT ICUB_FOUND)
#   MESSAGE (STATUS "ICUB repository not found; assuming distributed build.")
#   MESSAGE (STATUS "If this is not what you want, set ICUB_DIR variable.")
#ENDIF (NOT ICUB_FOUND)

SET(CONTROLBASIS_DIR "${ICUB_DIR}/src/controlBasis")

FIND_PACKAGE(CONTROLBASIS REQUIRED)
LINK_LIBRARIES(${CONTROLBASIS_LIBRARIES})
INCLUDE_DIRECTORIES(${CONTROLBASIS_INCLUDE_DIRS})

ADD_EXECUTABLE(${CB_ICUB_RESOURCE_MODULE} iCubControlBasisResources.cpp)
ADD_EXECUTABLE(${CB_RESOURCE_MODULE} controlBasisResource.cpp)
ADD_EXECUTABLE(${CB_CONTROL_MODULE} controlBasisControlLaw.cpp)
ADD_EXECUTABLE(${CB_SCHEMA_MODULE} controlBasisSchema.cpp)

INSTALL_TARGETS(/bin ${CB_RESOURCE_MODULE})
INSTALL_TARGETS(/bin ${CB_CONTROL_MODULE})
INSTALL_TARGETS(/bin ${CB_SCHEMA_MODULE})
INSTALL_TARGETS(/bin ${CB_ICUB_RESOURCE_MODULE})

