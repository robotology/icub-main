CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

SET(PROJECTNAME vergence)

PROJECT(${PROJECTNAME})

FIND_PACKAGE(YARP)
FIND_PACKAGE(ICUB)
SET(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} ${ICUB_DIR}/conf ${YARP_DIR}/conf)
FIND_PACKAGE(IPP REQUIRED)
FIND_PACKAGE(LOGPOLAR REQUIRED)
FIND_PACKAGE(IKIN)
FIND_PACKAGE(YARPMATH)

FILE(GLOB folder_source src/*.cpp src/*.cc src/*.c)
FILE(GLOB folder_header include/iCub/*.h)

SOURCE_GROUP("Source Files" FILES ${folder_source})
SOURCE_GROUP("Header Files" FILES ${folder_header})

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include
                    ${ICUB_INCLUDE_DIRS}
                    ${YARP_INCLUDE_DIRS}
		    ${YARPMATH_INCLUDE_DIRS}	
		    ${IKIN_INCLUDE_DIRS}
		    ${LOGPOLAR_INCLUDE_DIRS}
		    ${IPP_INCLUDE_DIRS}
		    )

MESSAGE("IKIN_INCLUDE_DIRS  ${IKIN_INCLUDE_DIRS} IKIN_LIBRARIES ${IKIN_LIBRARIES}  ")

ADD_EXECUTABLE(${PROJECTNAME} ${folder_source} ${folder_header})

TARGET_LINK_LIBRARIES(${PROJECTNAME} ${IKIN_LIBRARIES}
                                     ${YARP_LIBRARIES}
				     ${YARPMATH_LIBRARIES} 	
     				     ${LOGPOLAR_LIBRARIES}
				     ${ICUB_LIBRARIES}
				     ${IPP_LIBRARIES}
									 )

INSTALL(TARGETS ${PROJECTNAME} DESTINATION bin)
