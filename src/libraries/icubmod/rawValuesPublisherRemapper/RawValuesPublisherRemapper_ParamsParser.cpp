/*
 * SPDX-FileCopyrightText: 2023-2023 Istituto Italiano di Tecnologia (IIT)
 * SPDX-License-Identifier: LGPL-2.1-or-later
 */


// Generated by yarpDeviceParamParserGenerator (2.0)
// This is an automatically generated file. Please do not edit it.
// It will be re-generated if the cmake flag ALLOW_DEVICE_PARAM_PARSER_GERNERATION is ON.

// Generated on: Mon Sep 15 14:23:48 2025


#include "RawValuesPublisherRemapper_ParamsParser.h"
#include <yarp/os/LogStream.h>
#include <yarp/os/Value.h>

namespace {
    YARP_LOG_COMPONENT(RawValuesPublisherRemapperParamsCOMPONENT, "yarp.device.RawValuesPublisherRemapper")
}


RawValuesPublisherRemapper_ParamsParser::RawValuesPublisherRemapper_ParamsParser()
{
}


std::vector<std::string> RawValuesPublisherRemapper_ParamsParser::getListOfParams() const
{
    std::vector<std::string> params;
    params.push_back("axesNames");
    params.push_back("joints");
    return params;
}


bool RawValuesPublisherRemapper_ParamsParser::getParamValue(const std::string& paramName, std::string& paramValue) const
{
    if (paramName =="axesNames")
    {
        return false;
    }
    if (paramName =="joints")
    {
        paramValue = std::to_string(m_joints);
        return true;
    }

    yError() <<"parameter '" << paramName << "' was not found";
    return false;

}


std::string RawValuesPublisherRemapper_ParamsParser::getConfiguration() const
{
    //This is a sub-optimal solution.
    //Ideally getConfiguration() should return all parameters but it is currently
    //returning only user provided parameters (excluding default values)
    //This behaviour will be fixed in the near future.
    std::string s_cfg = m_provided_configuration;
    return s_cfg;
}

bool      RawValuesPublisherRemapper_ParamsParser::parseParams(const yarp::os::Searchable & config)
{
    //Check for --help option
    if (config.check("help"))
    {
        yCInfo(RawValuesPublisherRemapperParamsCOMPONENT) << getDocumentationOfDeviceParams();
    }

    m_provided_configuration = config.toString();
    yarp::os::Property prop_check(m_provided_configuration.c_str());
    //Parser of parameter axesNames
    {
        if (config.check("axesNames"))
        {
            {
                m_axesNames.clear();
                yarp::os::Bottle* tempBot = config.find("axesNames").asList();
                if (tempBot)
                {
                    std::string tempBots = tempBot->toString();
                    for (size_t i=0; i<tempBot->size(); i++)
                    {
                        m_axesNames.push_back(tempBot->get(i).asString());
                    }
                }
                else
                {
                     yCError(RawValuesPublisherRemapperParamsCOMPONENT) <<"parameter 'axesNames' is not a properly formatted bottle";
                }
            }
            yCInfo(RawValuesPublisherRemapperParamsCOMPONENT) << "Parameter 'axesNames' using value:" << m_axesNames;
        }
        else
        {
            yCError(RawValuesPublisherRemapperParamsCOMPONENT) << "Mandatory parameter 'axesNames' not found!";
            yCError(RawValuesPublisherRemapperParamsCOMPONENT) << "Description of the parameter: List of axes names";
            return false;
        }
        prop_check.unput("axesNames");
    }

    //Parser of parameter joints
    {
        if (config.check("joints"))
        {
            m_joints = config.find("joints").asInt64();
            yCInfo(RawValuesPublisherRemapperParamsCOMPONENT) << "Parameter 'joints' using value:" << m_joints;
        }
        else
        {
            yCInfo(RawValuesPublisherRemapperParamsCOMPONENT) << "Parameter 'joints' using DEFAULT value:" << m_joints;
        }
        prop_check.unput("joints");
    }

    /*
    //This code check if the user set some parameter which are not check by the parser
    //If the parser is set in strict mode, this will generate an error
    if (prop_check.size() > 0)
    {
        bool extra_params_found = false;
        for (auto it=prop_check.begin(); it!=prop_check.end(); it++)
        {
            if (m_parser_is_strict)
            {
                yCError(RawValuesPublisherRemapperParamsCOMPONENT) << "User asking for parameter: "<<it->name <<" which is unknown to this parser!";
                extra_params_found = true;
            }
            else
            {
                yCWarning(RawValuesPublisherRemapperParamsCOMPONENT) << "User asking for parameter: "<< it->name <<" which is unknown to this parser!";
            }
        }

       if (m_parser_is_strict && extra_params_found)
       {
           return false;
       }
    }
    */
    return true;
}


std::string      RawValuesPublisherRemapper_ParamsParser::getDocumentationOfDeviceParams() const
{
    std::string doc;
    doc = doc + std::string("\n=============================================\n");
    doc = doc + std::string("This is the help for device: RawValuesPublisherRemapper\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("This is the list of the parameters accepted by the device:\n");
    doc = doc + std::string("'axesNames': List of axes names\n");
    doc = doc + std::string("'joints': Number of control boards to be remapped\n");
    doc = doc + std::string("\n");
    doc = doc + std::string("Here are some examples of invocation command with yarpdev, with all params:\n");
    doc = doc + " yarpdev --device rawValuesPublisherRemapper --axesNames <mandatory_value> --joints 1\n";
    doc = doc + std::string("Using only mandatory params:\n");
    doc = doc + " yarpdev --device rawValuesPublisherRemapper --axesNames <mandatory_value>\n";
    doc = doc + std::string("=============================================\n\n");    return doc;
}
