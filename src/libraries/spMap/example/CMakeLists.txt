SET(KEYWORD "spmapmod")
PROJECT(${KEYWORD})

SET(YARP_DIR "$ENV{YARP_ROOT}" CACHE LOCATION "where is yarp?")
SET(ICUB_DIR "$ENV{ICUB_DIR}" CACHE LOCATION "where is iCub?")

FIND_PACKAGE(ICUB REQUIRED)
FIND_PACKAGE(YARP REQUIRED)
FIND_PACKAGE(OpenCV REQUIRED)
INCLUDE_DIRECTORIES(${OPENCV_INCLUDE_DIR})
LINK_LIBRARIES(${OPENCV_LIBRARIES})
FIND_PACKAGE(SPMAP REQUIRED)
INCLUDE_DIRECTORIES(${SPMAP_INCLUDE_DIRS})
LINK_LIBRARIES(${SPMAP_LIBRARIES})
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR})

FILE(GLOB folder_header *.h)
FILE(GLOB folder_source *.cpp)

ADD_EXECUTABLE(${KEYWORD} ${folder_source} ${folder_header})