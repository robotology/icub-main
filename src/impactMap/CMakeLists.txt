# replace "..." with the path to your YARP binaries if needed

SET(PROJECT_DIR ${CMAKE_SOURCE_DIR})
SET(HEADER_PATH "${PROJECT_DIR}/include")
SET(SRC_PATH "${PROJECT_DIR}/src")

FIND_PACKAGE(YARP REQUIRED)
FIND_PACKAGE(OpenCV REQUIRED)

FILE(GLOB CODEFILES ${SRC_PATH}/*.cpp)
#FILE(GLOB_RECURSE HEADERFILES ${HEADER_PATH}/*.h)

INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR} ${OPENCV_INCLUDE_DIR} ${HEADER_PATH})

LINK_DIRECTORIES(${OPENCV_LINK_DIRECTORIES})
LINK_LIBRARIES(${OPENCV_LIBRARIES})

#SOURCE_GROUP("Header Files" FILES ${HEADERFILES})
#SOURCE_GROUP("Source Files" FILES ${CODEFILES})

#ADD_EXECUTABLE(impactView src/ImpactView.cpp src/ImpactData.cpp)
ADD_EXECUTABLE(impactMap ${CODEFILES}) #src/main.cpp src/ImpactMapModule.cpp src/ImpactData.cpp)
