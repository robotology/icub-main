PROJECT(simio)

ADD_LIBRARY(simio simio.cpp simio.h)


#SET(Simio_INCLUDE_DIRS ${PROJECT_SOURCE_DIR})
#GET_TARGET_PROPERTY(SIMIO_LIBRARIES_RELEASE simio RELEASE_LOCATION)
#GET_TARGET_PROPERTY(SIMIO_LIBRARIES_DEBUG simio DEBUG_LOCATION)
#SET(SIMIO_LIBRARIES optimized ${SIMIO_LIBRARIES_RELEASE}
#		    debug ${SIMIO_LIBRARIES_DEBUG})

#IF (UNIX)
#   SET(SIMIO_LIBRARIES ncurses ${SIMIO_LIBRARIES})
#ENDIF (UNIX)

# Generate configuration information for this library.
#CONFIGURE_FILE(${PROJECT_SOURCE_DIR}/SIMIOConfig.cmake.in
#               ${PROJECT_BINARY_DIR}/SIMIOConfig.cmake
#	       @ONLY IMMEDIATE)

